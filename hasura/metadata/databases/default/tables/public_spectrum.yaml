table:
  schema: public
  name: spectrum
select_permissions:
  - role: anonymous
    permission:
      columns:
        - name
        - id
        - track_point_id
        - data
        - created_at
        - geometry
      filter: {}
      allow_aggregations: true
  - role: authorized
    permission:
      columns:
        - name
        - id
        - track_point_id
        - data
        - created_at
        - geometry
      filter: {}
      allow_aggregations: true
