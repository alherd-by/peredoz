table:
  schema: public
  name: spectrum
insert_permissions:
  - role: anonymous
    permission:
      check: {}
      columns:
        - id
        - name
        - data
        - created_at
        - track_point_id
        - geometry
      backend_only: false
select_permissions:
  - role: anonymous
    permission:
      columns:
        - name
        - id
        - track_point_id
        - data
        - created_at
        - geometry
      filter: {}
      allow_aggregations: true
  - role: authorized
    permission:
      columns:
        - name
        - id
        - track_point_id
        - data
        - created_at
        - geometry
      filter: {}
      allow_aggregations: true
