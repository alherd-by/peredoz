- name: allowed-queries
  definition:
    queries:
      - name: track
        query: |
          query track ($id: Int = 1) {
          	features_by_pk(id: $id) {
          		id
          		data
          	}
          }
